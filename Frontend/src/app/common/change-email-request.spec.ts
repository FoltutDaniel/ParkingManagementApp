import { ChangeEmailRequest } from './change-email-request';

describe('ChangeEmailRequest', () => {
  it('should create an instance', () => {
    expect(new ChangeEmailRequest()).toBeTruthy();
  });
});
